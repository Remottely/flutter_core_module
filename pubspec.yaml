name: flutter_core_module
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  # REMOTE CONNECTION
  http: ^0.13.4 #CORE
  hasura_connect: ^4.0.0+4 #CORE
  hive_cache_interceptor: ^1.0.0-dev.4 #CORE
  # ENVIRONMENT
  package_info_plus: ^3.0.1 #export
  # FIREBASE
  firebase_core: ^2.16.0
  # firebase_core: 1.10.0 #export
  firebase_core_web: ^2.8.0
  # firebase_core_web: 1.7.0 #export
  cloud_firestore: ^4.9.2
  # cloud_firestore: 2.5.3 #export
  firebase_storage: ^11.2.7
  # firebase_storage: 10.0.5 #export
  firebase_auth: ^4.10.0
  # firebase_auth: 3.3.8 #export
  firebase_analytics: ^10.5.0
  # firebase_analytics: #export
  cloud_functions: ^4.4.1
  # cloud_functions: 3.2.8 #export

  # ROUTER
  # go_router: ^6.0.1 #export
  go_router: ^11.1.1
  # go_router: ^3.0.5 #export #need to use this version to use widgetbook :/

  
  # STATE MANAGEMANT
  
  # CACHE
  localstorage: ^4.0.0+1 # lesnitsky.dev #export
  shared_preferences: ^2.0.3 # flutter.dev #export
  flutter_secure_storage: ^5.0.2 #export
  
  # SUPPORT
  zendesk_messaging: ^2.6.0+1 #export
  
  # HELPERS
  flutter_svg: ^1.0.3 #export
  yaml: ^3.1.0 #export
  provider:
  flutter_share: ^2.0.0

  # ANIMATIONS
  delayed_display: ^2.0.0 #export
  shimmer: ^2.0.0
  
  # DESIGN SYSTEM
  cupertino_icons: ^1.0.2 #export
  # google_fonts: ^2.0.0 #export
  # google_fonts: ^6.1.0 #export
  # mix: 0.0.6
  
  # BACKEND DYNAMIC WIDGET
  # json_dynamic_widget: ^7.0.0+2 #export
  automated_testing_framework: '^4.0.7+7' #export
  automated_testing_framework_plugin_images: '^4.0.0+24' #export
  desktop_window: '^0.4.0' #export

  # TODO
  # responsivity
  # skynexui_responsive_stylesheet: ^1.10.3
  # device
  # wakelock: ^0.6.1+2 # TODO
  # layout
  # flutter_staggered_grid_view: ^0.6.2 # TODO COMPANY DETAILS
  
dependency_overrides: 
  # path_provider: 2.0.0 #export
  # http_parser: ^4.0.0 #export

dev_dependencies:
  quiver: ^3.2.1 #CORE
  lints: ^2.0.0
  test: 
  mocktail: 0.1.4
  faker: 2.0.0
  network_image_mock: 2.0.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # To add assets to your package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/assets-and-images/#from-packages
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # To add custom fonts to your package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/custom-fonts/#from-packages
