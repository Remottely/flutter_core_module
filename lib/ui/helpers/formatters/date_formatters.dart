// import 'package:intl/intl.dart';

// class DateFormatters {
//   final DateFormat dayFormatter = DateFormat('dd');
//   final DateFormat monthFormatter = DateFormat('MM');
//   final DateFormat yearFormatter = DateFormat('yyyy');

//   String day(DateTime dt) => dayFormatter.format(dt);
//   String month(DateTime dt) => monthFormatter.format(dt);
//   String year(DateTime dt) => yearFormatter.format(dt);
// }
